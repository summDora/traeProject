import { createMockFn } from './mockFactory.js';

export const queryAsyncTaskList = createMockFn([]);
export const queryAsyncTaskStatus = createMockFn({});
export const retryAsyncTask = createMockFn({});
export const cancelAsyncTask = createMockFn({});
