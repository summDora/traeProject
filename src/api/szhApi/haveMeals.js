import { createMockFn } from '../mockFactory.js';

export const insertMeal = createMockFn([]);
export const queryMeal = createMockFn([]);
export const updateMeal = createMockFn([]);
export const queryMealDate = createMockFn([]);
export const meetingReplyDetail = createMockFn([]);
